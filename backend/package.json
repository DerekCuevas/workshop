{
  "name": "backend",
  "version": "0.0.1",
  "description": "",
  "scripts": {
    "build": "tsc",
    "dev": "tsc && concurrently \"tsc -w\" \"nodemon lib/index.js\"",
    "start": "npm run build && node ./lib/index.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^5.2.0",
    "nodemon": "^2.0.3",
    "prettier": "^2.0.5",
    "typescript": "^3.8.3"
  },
  "dependencies": {
    "apollo-server": "^2.12.0",
    "graphql": "^15.0.0"
  }
}
